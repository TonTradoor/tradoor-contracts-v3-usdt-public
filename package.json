{
    "name": "tradoor-contracts",
    "version": "0.0.1",
    "scripts": {
        "start": "blueprint run",
        "build": "blueprint build --all",
        "test": "jest --verbose --maxWorkers=1",
        "test-build": "yarn build && yarn test",
        "runmain": "blueprint run --mainnet --mnemonic",
        "rundev": "blueprint run --testnet --mnemonic",
        "deploy": "sh scripts/deploy.sh",
        "deploymain": "sh scripts/deployMain.sh",
        "deploy-build": "yarn build && yarn deploy"
    },
    "devDependencies": {
        "@orbs-network/ton-access": "^2.3.3",
        "@ton/blueprint": "^0.17.0",
        "@ton/core": "^0.56.0",
        "@ton/crypto": "^3.2.0",
        "@ton/sandbox": "^0.19.0",
        "@ton/test-utils": "^0.4.2",
        "@ton/ton": "^13.11.0",
        "@types/jest": "^29.5.11",
        "@types/node": "^20.11.7",
        "jest": "^29.7.0",
        "prettier": "^3.2.4",
        "ts-jest": "^29.1.2",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "@aws-crypto/sha256-js": "^5.2.0",
        "decimal.js": "^10.4.3"
    }
}
